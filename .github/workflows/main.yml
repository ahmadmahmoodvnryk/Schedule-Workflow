name: Schedule jobs

on: 
 schedule:
   - cron: "*/5 * * * *"

jobs:
  run-script:
   runs-on: ubuntu-latest

   steps:
     - name: Print Message
       run: echo "Hello! Schedule Workflow Executed"
